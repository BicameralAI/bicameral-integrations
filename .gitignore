# Sibling tools — local process/AI tooling (see docs/governance/SIBLINGS.md).
# Never tracked, never referenced.
.agent/
.agents/
.claude/
.failsafe/
.qor/
.bicameral/
CLAUDE.md
GEMINI.md
COPILOT.md
CURSOR.md
RUN_SUMMARY.md

# Planning drafts
plan-*.md
Planning/

# IDE local settings
.vscode/
.idea/
.cursor/
.windsurf/

# Operator-local config secrets — NEVER commit (glob block; the example stays committed via negation)
config/bicameral.local*.json
config/*.local.json
config/secrets*.json
!config/bicameral.example.json

# Python local artifacts
.pytest_cache/
__pycache__/
*.py[cod]
.venv/
venv/
# qor:seed -- do not edit between markers
.qor/session/
# qor:seed/end
.codex/
