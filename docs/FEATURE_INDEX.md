# Bicameral Integrations Feature Index

Single canonical cross-reference of every user-touchable feature in Bicameral Integrations against documentation, source code, and test surface. Updated per the Phase 73 FEATURE_INDEX update obligation in every `/qor-implement` cycle (see `/qor-implement` Step 12.5).

**Generated**: 2026-06-02T03:14:31.4698244-04:00 by `qor-bootstrap`
**Sources**: declared by `/qor-plan` `Feature Inventory Touches` table per cycle.

## Coverage Summary

- Total entries: **48**
- **Verified**: 48
- **Unverified**: 0
- **N/A (operator-justified)**: 0

---

## Section: Integrations

| ID | Feature | Doc | Code | Test | Status | Notes |
|---|---|---|---|---|---|---|
| FX-ADP-001 | Universal adapter normalization seam (`normalize` + `validate_emissions`) | docs/plan-adapter-core-github-connector-2026-06-02.md, docs/adr/0014-emission-metadata-preservation.md | adapter/core/pipeline.py, adapter/core/observations.py | adapter/core/tests/test_pipeline.py | Verified | Observation→AdapterEmission seam; enforces ADR-0005 contract rules. **Metadata preservation (ADR-0014, 2026-06-08):** `_emission_from` now carries `dict(obs.metadata)` into `AdapterEmission.metadata` (defensive copy) — previously dropped, silently starving mods of structured connector signals (OSV `severity`/`packages`, ServiceNow `number`/`state`, …). Emission-level; in-process for mods only (NOT wire-forwarded — `gateway_mapping` omits metadata). Round-trip + identity-copy regression-locked. |
| FX-GH-001 | GitHub PR → Observation parser | docs/plan-adapter-core-github-connector-2026-06-02.md | connectors/github/connector.py | connectors/github/tests/test_github_connector.py | Verified | Fixture-based; live fetch_active/webhook deferred (no live API this cycle) |
| FX-SEC-001 | Producer sensitive-data screen (secret/PHI/PAN) | docs/plan-security-governance-alignment-2026-06-03.md | adapter/core/sensitive.py, adapter/core/pipeline.py | adapter/core/tests/test_sensitive.py, adapter/core/tests/test_pipeline.py | Verified | Port of mcp sensitive_patterns; HARD-gate in validate_emissions. **Red-team Cycle A (Entry #86):** screen now covers EVERY wire-bound field — `source_id` (→ `source_type`) + `source_ref.url`/`ref`/`source_id` — closing the secret-in-URL→gateway leak (#52); `_redact_excerpt` masks pan/phi/short-secret so a rejected emission's error never carries a raw PAN/MRN (#53). **Cycle B (Entry #88):** email redactor uses RFC-bounded quantifiers (linear, no ReDoS — #51, SG-2026-06-05-F); cursor redacts free-text `day`/`mostUsedModel` (#58). **Cycle C (Entry #90):** rejects a zero-width-only excerpt (Unicode `Cf` treated as blank) + bounds `source_id` to 128 chars (#61). **Metadata surface (ADR-0014, 2026-06-08):** `emission.metadata` (newly preserved) is screened **per leaf** via `_metadata_strings` (every string leaf + dict KEY, recursing dict/list/tuple/set, stringifying non-str scalars). Per-leaf — NOT a single join — because a join would fabricate `_is_id_preceded` PAN suppression across two independent leaves (false negative); within-leaf id-label suppression still honoured. Flat/nested/key/scalar/tuple-set/no-false-adjacency all regression-locked. |
| FX-FATHOM-001 | Fathom meeting → Observation parser | docs/plan-source-connectors-fathom-linear-ports-2026-06-03.md | connectors/fathom/connector.py | connectors/fathom/tests/test_fathom_connector.py | Verified | Net-new (developers.fathom.ai); transcript→excerpt (summary/title fallback); PASSIVE+WEBHOOK declared, live REST/Svix-verify deferred |
| FX-LINEAR-001 | Linear webhook event → Observation parser | docs/plan-source-connectors-fathom-linear-ports-2026-06-03.md | connectors/linear/connector.py | connectors/linear/tests/test_linear_connector.py | Verified | Net-new (linear.app/developers); Issue envelope, action/type→metadata; WEBHOOK+ACTIVE declared, live GraphQL/Linear-Signature-verify deferred |
| FX-GRANOLA-001 | Granola transcript → Observation parser | docs/plan-source-connectors-fathom-linear-ports-2026-06-03.md | connectors/granola/connector.py | connectors/granola/tests/test_granola_connector.py | Verified | Port of mcp events/sources/granola.py; PASSIVE; live poll/watermark deferred |
| FX-LOCALDIR-001 | Local-directory file → Observation parser | docs/plan-source-connectors-fathom-linear-ports-2026-06-03.md | connectors/local_directory/connector.py | connectors/local_directory/tests/test_local_directory_connector.py | Verified | Port of mcp events/sources/local_directory.py; PASSIVE; sha256 path token ref; live scan/watermark deferred |
| FX-GDRIVE-001 | Google Docs document → Observation parser | docs/plan-source-connectors-fathom-linear-ports-2026-06-03.md | connectors/google_drive/connector.py | connectors/google_drive/tests/test_google_drive_connector.py | Verified | Port of mcp google_drive adapter; structured-body flatten; ACTIVE; _walk_table refactored ≤3 nesting. **Live fetch built 2026-06-08 → FX-GDRIVE-002** (stale "deferred" docstring corrected; connector stays a pure parse surface) |
| FX-GDRIVE-002 | Google Docs `documents.get` ACTIVE single-document live-fetch path (the deferred half → go-live coding) | docs/plan-google-drive-live-2026-06-08.md | runtime/doc_fetch.py, runtime/poll_specs.py (`build_google_drive_spec`) | runtime/tests/test_doc_fetch.py | Verified | `fetch_document(spec, transport, sink)` — the **single-resource GET** fetch shape (a third module beside REST `poll_client` and `graphql_poll`: one GET → one Document → one Observation; no pagination). **Doc-verified (developers.google.com/docs/api, 2026-06-08):** `GET https://docs.googleapis.com/v1/documents/{documentId}`, `Authorization: Bearer <access_token>`, scopes `documents.readonly`/`drive.readonly`/`drive.file`. **Security:** `document_id` is **`re.fullmatch`-validated** (`[A-Za-z0-9_-]{1,200}`) BEFORE the URL splice (path/URL-injection guard — a half-anchored match would admit `x/../y`/`x?a=b`/`x@host`/`x\r\n`; fail-closed `bad_document_id`, no secret resolved, no request); explicit local `_MAX_RESPONSE` body cap IN `fetch_document` (the RecordedTransport doesn't cap); **dict-ONLY** decode (rejects a list/scalar/null 200 — the sole guard, since `parse_document` is non-self-guarding); **FX-SEC-001** via the real `pipeline.normalize` (a secret in the doc text is HARD-rejected before `sink.emit`, no partial — regression-locked); token never in a `PollError`. **OAuth boundary:** `SecretResolver` returns a valid access token (refresh stays operator-runtime); HTTP boundary operator-run (recorded transport proves the path; a mock does NOT promote to Live — ADR-0012). Wire-gate (auth.md): multi-tab docs (`tabs[]` vs legacy `body` → title-only) + Bearer header confirmed live before the flip. Folder-polling (`files.list`) + push webhooks deferred. Independent audit (VETO→addressed: fullmatch guard, parametrized injection test, oversized-cap-in-fetch, dict-only) + pre-seal devil's-advocate. 13 tests. L2 |
| FX-WHSEC-001 | Webhook signature verification + delivery dedup (Svix + hex HMAC + multi-sig) | docs/plan-webhook-verification-dedup-2026-06-04.md | adapter/core/webhook_security.py | adapter/core/tests/test_webhook_security.py | Verified | `verify_standard_webhook` (Svix/Fathom), `verify_hmac_hex` (Linear/Sentry), **`verify_hmac_hex_multi`** (PagerDuty `v1=` rotation membership), `DeliveryDedupCache`; constant-time, fail-closed on all attacker-input paths; L3 |
| FX-WHSEC-002 | Slack `v0` request-signature primitive | docs/plan-webhook-verify-cohort-2026-06-04.md | adapter/core/webhook_security.py | adapter/core/tests/test_webhook_security.py | Verified | `verify_slack_signature` — HMAC-SHA256 over `v0:{ts}:{raw_body}`, `v0=`-prefixed, 300 s replay window (raw timestamp string in basestring); fail-closed on missing/non-numeric ts, stale, missing sig/secret, mismatch; constant-time; L3 |
| FX-GH-002 | GitHub webhook verify + envelope unwrap + dedup | docs/plan-webhook-verify-cohort-2026-06-04.md | connectors/github/connector.py | connectors/github/tests/test_github_webhook.py | Verified | `GitHubConnector.verify`/`normalize_event` (`X-Hub-Signature-256` `sha256=` hex HMAC over raw body, fail-closed; `X-GitHub-Delivery` dedup); unwraps the PR envelope and injects the top-level `number` (test asserts `owner/repo#92` survives); live REST fetch deferred |
| FX-SLACK-002 | Slack webhook verify + dedup wiring | docs/plan-webhook-verify-cohort-2026-06-04.md | connectors/slack/connector.py | connectors/slack/tests/test_slack_webhook.py | Verified | `SlackConnector.verify`/`normalize_event` (`verify_slack_signature` + `X-Slack-Request-Timestamp` 5 m window; `event_id` dedup; signed `url_verification` handshake → `[]`); stale + tamper-with-fresh-timestamp both rejected; live Events-API receipt deferred |
| FX-NOTION-002 | Notion webhook verify + dedup wiring | docs/plan-webhook-verify-cohort-2026-06-04.md | connectors/notion/connector.py | connectors/notion/tests/test_notion_webhook.py | Verified | `NotionConnector.verify`/`normalize_event` (`X-Notion-Signature` `sha256=` hex HMAC over raw body with the verification token; **prefix required** — bare hex rejected; best-effort dedup); live API fetch/OAuth deferred |
| FX-FATHOM-002 | Fathom webhook verify + dedup wiring | docs/plan-webhook-verification-dedup-2026-06-04.md | connectors/fathom/connector.py | connectors/fathom/tests/test_fathom_webhook.py | Verified | `FathomConnector.verify`/`normalize_event` (Svix; injected secret/clock/dedup; self-guarded); live HTTP deferred |
| FX-LINEAR-002 | Linear webhook verify + dedup wiring | docs/plan-webhook-verification-dedup-2026-06-04.md | connectors/linear/connector.py | connectors/linear/tests/test_linear_webhook.py | Verified | `LinearConnector.verify`/`normalize_event` (Linear-Signature HMAC-first + 60s anti-replay; injected secret/clock/dedup; self-guarded); webhook receive path Beta-proven (FX-RUNTIME-001). **GraphQL active fetch built 2026-06-08 → FX-LINEAR-003** (the stale "deferred" docstring corrected) |
| FX-LINEAR-003 | Linear ACTIVE GraphQL live-fetch path (the deferred half → go-live coding) | docs/plan-linear-graphql-live-2026-06-08.md | runtime/graphql_poll.py, runtime/poll_specs.py (`build_linear_graphql_spec`), connectors/linear/connector.py (`parse_issue_node`) | runtime/tests/test_graphql_poll.py, connectors/linear/tests/test_linear_connector.py | Verified | `poll_graphql(spec, transport, sink)` — the GraphQL counterpart of the REST `poll_client.poll` (a NEW module; the sealed REST client is untouched). **Doc-verified contract (linear.app/developers, 2026-06-08):** POST `https://api.linear.app/graphql`, **API key in `Authorization: <key>` (raw, NO Bearer prefix)**, Relay cursor `first`/`after` (default 50), envelope `data.issues.{nodes,pageInfo{hasNextPage,endCursor}}`. **GraphQL-specific fail-closed (the divergences from REST):** cursor rides in the request **body** (`variables.after`, re-serialized per page — proven: page 2 carries page 1's `endCursor`); a **200 carrying an `errors` array does NOT emit** (no partial); **rate-limit is HTTP 400 + `errors[].code=="RATELIMITED"`** (not 429) → backpressure `PollError`; **explicit body cap in `poll_graphql`** (the RecordedTransport doesn't cap); defensive 400-body parse (non-JSON/non-list-`errors` → `http_error`, no crash); runaway-cursor stop (truthy `hasNextPage` + empty `endCursor` → STOP, mirrors `PageToken`); `_MAX_PAGES` cap; cursor screened on read (`_reject_control_chars` — though it rides in the JSON body, a data-integrity guard). **FX-SEC-001 enforced** via the real `pipeline.normalize` (a secret in an issue `description` is HARD-rejected, never emitted — regression-locked) + `GatewaySink` re-screen at the Live boundary. Secret resolved by `source_id="linear"`, never in a `PollError`. `parse_issue_node` (GraphQL Issue node, distinct from the webhook envelope; PII-safe — no assignee/creator identity). HTTP boundary operator-run (recorded transport proves the path; a mock does NOT promote to Live — ADR-0012); wire-gate (issue field set) recorded in `auth.md` before the live flip. Independent audit (VETO→addressed: explicit body cap, FX-SEC-001 proof, defensive 400 parse) + pre-seal devil's-advocate. L2 |
| FX-SARIF-001 | SARIF 2.1.0 result → Observation parser | docs/plan-connectors-phase1-2026-06-04.md | connectors/sarif/connector.py | connectors/sarif/tests/test_sarif_connector.py | Verified | Catalog P0 (security/compliance-evidence, T0); one Observation per `runs[].results[]`; file import only, live CI-collection deferred |
| FX-SLACK-001 | Slack message event → Observation parser | docs/plan-connectors-phase1-2026-06-04.md | connectors/slack/connector.py | connectors/slack/tests/test_slack_connector.py | Verified | Catalog P0 (communication, T2); `event_callback` envelope or bare message; edit-subtype nested-message unwrap; pinned non-empty excerpt fallback; live Events-API/signature-verify + notify/write (T3+) deferred |
| FX-NOTION-001 | Notion page → Observation parser | docs/plan-connectors-phase1-2026-06-04.md | connectors/notion/connector.py | connectors/notion/tests/test_notion_connector.py | Verified | Catalog P0 (docs, T1); title via `type=="title"` property (id → `notion-page` terminal floor); ACTIVE+WEBHOOK declared, live API/OAuth/block-fetch deferred |
| FX-MCPREG-001 | MCP Registry server entry → Observation parser | docs/plan-connectors-phase1-2026-06-04.md | connectors/mcp_registry/connector.py | connectors/mcp_registry/tests/test_mcp_registry_connector.py | Verified | Catalog P0 (mcp/agent-ecosystem, T1); `server.json` title/description→excerpt, repository.url→url; ACTIVE; read-only scoring/allowlist, live registry-fetch deferred |
| FX-CONTINUE-001 | Continue dev-data event → Observation parser | docs/plan-connectors-dev-tools-2026-06-04.md | connectors/continue_dev/connector.py | connectors/continue_dev/tests/test_continue_connector.py | Verified | Catalog P1 (developer-AI tooling, T0); dev-data JSONL event, prompt/completion→excerpt with `continue {name}` terminal floor; PASSIVE; package `continue_dev` (keyword), source_id `continue`; live file-watch/HTTP-sink/Hub deferred |
| FX-AIDER-001 | Aider attributed git commit → Observation parser | docs/plan-connectors-dev-tools-2026-06-04.md | connectors/aider/connector.py | connectors/aider/tests/test_aider_connector.py | Verified | Catalog P1 (developer-AI tooling, T0); `(aider)` author/committer or Co-authored-by trailer → attributed_by; subject→excerpt with hash→`aider-commit` floor; PASSIVE; live git-log walk + analytics/chat-history deferred |
| FX-OSV-001 | OSV.dev vulnerability record → Observation parser | docs/plan-connectors-phase2-2026-06-04.md | connectors/osv/connector.py | connectors/osv/tests/test_osv_connector.py | Verified | Catalog P0 (security-evidence, T1 no-auth); supply-chain aggregator (GHSA/PyPA/RustSec); summary→excerpt with details→id floor; ACTIVE; SG-I defensive (all-optional schema); live query client deferred |
| FX-SENTRY-001 | Sentry issue webhook → Observation parser | docs/plan-connectors-phase2-2026-06-04.md | connectors/sentry/connector.py | connectors/sentry/tests/test_sentry_connector.py | Verified | Catalog P1 (observability/incident, T1); `data.issue` unwrap; title→excerpt with culprit/shortId/id floor; WEBHOOK; live Events-API/`Sentry-Hook-Signature` verify deferred |
| FX-PAGERDUTY-001 | PagerDuty v3 incident webhook → Observation parser | docs/plan-connectors-phase2-2026-06-04.md | connectors/pagerduty/connector.py | connectors/pagerduty/tests/test_pagerduty_connector.py | Verified | Catalog P1 (observability/incident, T1); nested `event.data` unwrap; title→excerpt with summary/id floor; WEBHOOK |
| FX-SENTRY-002 | Sentry webhook verify + dedup wiring | docs/plan-webhook-hardening-2026-06-04.md | connectors/sentry/connector.py | connectors/sentry/tests/test_sentry_connector.py | Verified | `SentryConnector.verify`/`normalize_event` (hex HMAC over RAW body; injected secret/dedup; self-guarded; best-effort dedup, no replay window per Sentry); live HTTP receipt deferred |
| FX-PAGERDUTY-002 | PagerDuty webhook verify + dedup wiring | docs/plan-webhook-hardening-2026-06-04.md | connectors/pagerduty/connector.py | connectors/pagerduty/tests/test_pagerduty_connector.py | Verified | `PagerDutyConnector.verify`/`normalize_event` (multi-signature `v1=` membership via `verify_hmac_hex_multi`; injected secret/dedup; self-guarded); live HTTP receipt deferred; first-party scheme spot-check pending (BACKLOG) |
| FX-CLAUDECODE-001 | Claude Code transcript line → Observation parser | docs/plan-claude-code-2026-06-04.md | connectors/claude_code/connector.py | connectors/claude_code/tests/test_claude_code_connector.py | Verified | Catalog P0 (developer-AI tooling, T0); `~/.claude/**/*.jsonl` heterogeneous event log → `parse_session_line` filters to user/assistant/summary (meta/unknown→None); `[claude-code:{kind}] {uuid}` terminal floor; depth-capped + type-defensive (SG-I); PASSIVE; live file-watch/history/git-attribution deferred |
| FX-JIRA-001 | Jira Cloud issue webhook → Observation parser + verify | docs/plan-jira-2026-06-04.md | connectors/jira/connector.py | connectors/jira/tests/test_jira_connector.py | Verified | Catalog P0 (project-management, T1); `parse_issue` (summary→excerpt, never ADF description, `jira-issue` floor); `JiraConnector.verify`/`normalize_event` (`X-Hub-Signature` `sha256=` hex HMAC over raw body, fail-closed; best-effort dedup on `X-Atlassian-Webhook-Identifier`); WEBHOOK+ACTIVE; live HTTP/REST + Connect-JWT deferred |
| FX-WHSEC-003 | Zendesk Base64 request-signature primitive | docs/plan-zendesk-connector-2026-06-04.md | adapter/core/webhook_security.py | adapter/core/tests/test_webhook_security.py | Verified | `verify_zendesk_signature` — **Base64** HMAC-SHA256 over `timestamp + body` (no separator; raw timestamp string; empty body accepted for GET/DELETE); fail-closed on missing secret/sig/timestamp, mismatch; constant-time; no window (dedup-as-replay-guard); L3 |
| FX-ZENDESK-001 | Zendesk support-ticket webhook → Observation parser + verify | docs/plan-zendesk-connector-2026-06-04.md | connectors/zendesk/connector.py | connectors/zendesk/tests/test_zendesk_connector.py | Verified | Catalog P1 (support/CS, T1); `parse_ticket` (subject→excerpt, **never the ticket body** — PII-dense; `zendesk-ticket` floor, id via `detail.id` or `zen:ticket:<id>` subject parse; SG-I defensive); `ZendeskConnector.verify`/`normalize_event` (`X-Zendesk-Webhook-Signature` Base64 HMAC over `timestamp+body`, fail-closed; best-effort dedup); WEBHOOK+ACTIVE; **ticket body (`detail.description`) now ingested via redact-and-pass** (Entry #84) — `subject — redact(description)`, secret/PHI/PAN/email/phone scrubbed, FX-SEC-001 backstop; live REST/OAuth deferred |
| FX-GITLAB-001 | GitLab merge-request / issue webhook → Observation parser + plaintext-token verify | docs/plan-source-connectors-gitlab-confluence-2026-06-05.md | connectors/gitlab/connector.py | connectors/gitlab/tests/test_gitlab_connector.py | Verified | Catalog P1 (source-control, T1/T3); `parse_merge_request`/`parse_issue` (`object_attributes.description`→excerpt, `!iid`/`#iid` ref, `gitlab-merge-request`/`gitlab-issue` floor); `observations` dispatch on `object_kind`; `GitLabConnector.verify`/`normalize_event` (**plaintext `X-Gitlab-Token` shared-secret, constant-time, fail-closed — NOT HMAC**; new `verify_shared_token` helper; best-effort dedup on `X-Gitlab-Event-UUID`); WEBHOOK+ACTIVE; harness-proven webhook + fail-closed negatives; Standard-Webhooks signing-token path + live REST deferred |
| FX-CONFLUENCE-001 | Confluence Cloud page content → Observation parser (verify deferred) | docs/plan-source-connectors-gitlab-confluence-2026-06-05.md | connectors/confluence/connector.py | connectors/confluence/tests/test_confluence_connector.py | Verified | Catalog P1 (documentation, T1/T3); `parse_content` (`body.storage.value` XHTML flattened via lossy `_strip_storage_html` — tags/entities/whitespace, **not a sanitizer**; `confluence-page` floor; `_links.base+webui` url); ACTIVE+PASSIVE+WEBHOOK; **verify deferred** (Cloud signature scheme unverifiable from docs — Data-Center-only `X-Hub-Signature`); poll-harness-proven |
| FX-COPILOT-001 | GitHub Copilot aggregate usage-metrics day → Observation parser | docs/plan-source-connectors-copilot-cursor-2026-06-05.md | connectors/copilot/connector.py | connectors/copilot/tests/test_copilot_connector.py | Verified | Catalog P1 (developer-AI, T1); `parse_metrics_day` summarizes `GET /orgs/{org}/copilot/metrics` aggregate counts (active/engaged + IDE-completions/IDE-chat/dotcom-chat/PR-summaries engaged) into a PII-free excerpt; `copilot:metrics:<date>` ref, `copilot-metrics` floor; ACTIVE poll (no webhooks); **aggregate object is PII-free by design** (per-user NDJSON report deferred); live REST poll + `manage_billing:copilot`/`read:org` token deferred |
| FX-CURSOR-001 | Cursor team daily-usage row → PII-free aggregate Observation parser | docs/plan-source-connectors-copilot-cursor-2026-06-05.md | connectors/cursor/connector.py | connectors/cursor/tests/test_cursor_connector.py | Verified | Catalog P1 (developer-AI, T1); `parse_usage_day` summarizes `POST /teams/daily-usage-data` aggregate metrics (accepted lines, accepts/applies, agent/chat/composer requests, mostUsedModel) via a strict allowlist; **drops `email`/`name`/`clientVersion` at parse time** (FX-SEC-001 screens secret/PHI/PAN only, NOT generic email — SG-2026-06-05-A); **per-developer attribution via the OPAQUE `userId`** (Entry #84, SG-2026-06-05-D supersedes -A for userId only — pseudonymous; email/name still never read); non-vacuous PII test (email/name absent) + opaque-userId-surfaced test; ACTIVE poll (no webhooks); live REST poll + basic-auth API key deferred |
| FX-REDACT-001 | PII redaction-and-pass primitive (`redact` + `redact_catalog`) | docs/plan-pii-redaction-devin-servicenow-2026-06-05.md | adapter/core/redaction.py | adapter/core/tests/test_redaction.py | Verified | `redact(text)` scrubs secret/PHI/PAN (value-consuming `redact_catalog` in `sensitive.py`) + email/phone to placeholders so free-text PASSES `_screen_sensitive` (redact-and-pass); composes WITH — never replaces — FX-SEC-001 (the hard backstop). **Keystone invariant `detect_sensitive(redact(x)) == []` holds by construction** (catalog runs LAST after email/phone); proven on an adversarial corpus (phone-abutting-PAN, id-shielded PAN with intervening PHI label, concatenated phone+PAN). stdlib `re` only; irreversible placeholders. Detection patterns unchanged (additive) |
| FX-DEVIN-001 | Devin v3 agentic-session → redacted Observation parser | docs/plan-pii-redaction-devin-servicenow-2026-06-05.md | connectors/devin/connector.py | connectors/devin/tests/test_devin_connector.py | Verified | Catalog P1 (developer-AI/agentic, T1); `parse_session` over `GET /v3/organizations/{org}/sessions` (Bearer `cog_` key); free-text (title/structured_output) passed through `redact()` (session trail may carry secrets/PII); `pull_request.url` kept as artifact location (github/gitlab/jira precedent); author identity not read; ACTIVE poll (no webhooks); harness-proven (body redacted, no `@example.com`); live REST poll + `/messages` surface deferred |
| FX-SERVICENOW-001 | ServiceNow incident → redact-and-pass Observation parser | docs/plan-pii-redaction-devin-servicenow-2026-06-05.md | connectors/servicenow/connector.py | connectors/servicenow/tests/test_servicenow_connector.py | Verified | Catalog P2 (ITSM, T1); `parse_incident` over Table API `GET /api/now/table/incident`; metadata surface (number/short_description/state/priority) + `description` through `redact()`; **`caller_id` identity never read**; end-to-end **redact-and-pass proof** (a valid-shape `AKIA` secret in the description WOULD be rejected raw — companion assertion — but is redacted so the emission passes FX-SEC-001); ACTIVE poll (no webhooks); live Table API poll + basic/OAuth deferred |
| FX-OPENAI-ADMIN-001 | OpenAI org audit-log event → governance Observation (actor dropped) | docs/plan-source-connectors-openai-anthropic-admin-2026-06-05.md | connectors/openai_admin/connector.py | connectors/openai_admin/tests/test_openai_admin_connector.py | Verified | Catalog P1 (developer-AI/governance, T1); `parse_audit_log` over `GET /v1/organization/audit_logs` (Bearer admin key); excerpt = event type + project + non-PII `actor.type` (allowlisted session/api_key) + UTC time; **actor identity NEVER read** (`actor.*.user.email`/`.id`, `session.ip_address`) — sole control, since FX-SEC-001 screens secret/PHI/PAN not generic email/IP and `redact()` has no IPv4 scrub (SG-2026-06-05-C); non-vacuous drop test (fixture carries email+IP) + end-to-end harness (no `@example.com`/IP); ACTIVE poll; live REST poll + admin key deferred |
| FX-ANTHROPIC-ADMIN-001 | Anthropic org usage bucket → aggregate PII-free Observation | docs/plan-source-connectors-openai-anthropic-admin-2026-06-05.md | connectors/anthropic_admin/connector.py | connectors/anthropic_admin/tests/test_anthropic_admin_connector.py | Verified | Catalog P1 (developer-AI/leverage, T1); `parse_usage` over `GET /v1/organizations/usage_report/messages`; sums input (`uncached`+`cache_read`+`cache_creation`) + output tokens and collects distinct models across a bucket's `results`; **aggregate, PII-free** (opaque `workspace_id`/`api_key_id` not surfaced); ACTIVE poll (no webhooks); per-user Claude Code Analytics + `/cost_report` deferred; live REST poll + `x-api-key` admin key deferred |

---

## Section: Runtime Boundary

| ID | Feature | Doc | Code | Test | Status | Notes |
|---|---|---|---|---|---|---|
| FX-RUNTIME-001 | Operator-runtime boundary layer (sinks + delivery + secret resolver) | docs/adr/0012-connector-readiness-ladder-and-live-ingest-runtime.md, docs/plan-go-live-runtime-2026-06-04.md | runtime/sinks.py, runtime/delivery.py, runtime/secrets.py | runtime/tests/test_runtime.py | Verified | `EmissionSink`/`SecretResolver` Protocols + `CollectingSink` + `GatewaySink` (#109-gated stub raising `GatewayEmissionGated`) + `deliver_webhook`/`deliver_poll`; drives connector ingest→verify→normalize→emit without the repo being a server (ADR-0012). **Beta cohort proven through the harness**: signed-webhook→1 + bad-sig→0 for **linear** (HMAC+replay), **fathom** (Svix), **sentry** (hex HMAC), **pagerduty** (multi-sig membership — valid sig placed 2nd), **github** (sha256= + envelope unwrap), **slack** (v0 basestring), **notion** (sha256= prefix-pinned), **zendesk** (Base64 over timestamp+body), **jira** (sha256=); **deliver_poll** earns Beta for the non-webhook connectors — granola/local_directory/google_drive/sarif(→2)/mcp_registry/continue/aider/claude_code(→4, filtered)/osv(→2); **all 18 connectors Beta**; GatewaySink raises when unconfigured. **Red-team Cycle B (Entry #88):** `deliver_webhook` caps body at 1 MiB + 9 connectors catch `ValueError` (huge-int json.loads fails closed — #55); github/servicenow nested-field guards (#56); fathom `verify()` fail-closed on malformed header types (#57); all 26 `observations()` reject a non-dict payload (#59). **Cycle C (Entry #90):** the 4 windowless providers dedup id-less deliveries by body hash, closing the unbounded id-less replay (#60; bounded-cache eviction/TTL residual documented) |
| FX-RUNTIME-002 | Live emission seam — AdapterEmission → v1 IngestRequest + GatewaySink POST | docs/adr/0012-connector-readiness-ladder-and-live-ingest-runtime.md, docs/plan-live-emission-2026-06-05.md | runtime/gateway_mapping.py, runtime/sinks.py, runtime/schemas/ingest_request_v1.schema.json | runtime/tests/test_gateway_mapping.py | Verified | `emission_to_ingest_request` maps to the pinned/vendored v1 `IngestRequest` (title/description/source floored; evidence excerpt; dimensional confidence NOT collapsed — SG-2026-06-02-B). `GatewaySink` real `POST /api/v1/ingest` (stdlib urllib): **default-safe** (no endpoint → `GatewayEmissionGated`), **fail-closed** (re-screens at the boundary; only HTTP 201 succeeds; else `GatewayEmissionError(status, reason)`), **secret-safe** (operator token never in any error/log; **red-team Cycle A / Entry #86**: `__init__` rejects CR/LF in token/headers with a value-free error + `_post` token-free catch-all so a CR/LF token can't leak via an uncaught `ValueError` — #54). Bot #109 landed (PR #131); Live is now operator-actionable. L3 |
| FX-RUNTIME-003 | Live-poll client — the ingest *fetch* half for ACTIVE connectors (request construction + auth + pagination), the symmetric counterpart of `deliver_webhook`'s receive side | docs/adr/0012-connector-readiness-ladder-and-live-ingest-runtime.md, docs/plan-runtime-live-poll-client-2026-06-06.md, docs/plan-poll-client-bearer-connectors-2026-06-06.md | runtime/poll_client.py, runtime/poll_auth.py, runtime/poll_specs.py | runtime/tests/test_poll_client.py | Verified | `poll(connector, spec, transport, sink)` walks pagination via an injected `HttpTransport` (stdlib `UrllibTransport` default; a `RecordedTransport` proves the path over captured fixtures — a mock does NOT promote to Live, ADR-0012) then delegates to `deliver_poll` (parse→normalize→emit). Auth (`poll_auth.py`): `ApiKeyHeaderAuth` + `BearerAuth` + `BasicAuth` (CR/LF-rejecting raw inputs, **token-free errors** mirroring `GatewaySink` #54); pagination (`poll_client.py`): `PageToken` (token/cursor; provider token treated as **untrusted** — control-char reject + URL-encode) + `OffsetPager` (`sysparm_offset`-style; stop-on-short-page, offset-parse fail-closed); page body may be a JSON **object OR top-level array**; GET **and POST body**; **fail-closed** on non-200 / unparseable / non-object body / non-list items / poisoned token / blank secret (no request) / `_MAX_PAGES`=100 / `_MAX_RESPONSE`=8 MiB. Per-connector specs (`poll_specs.py`) resolve the secret by **`source_id`** and wire endpoint+auth+pagination: **anthropic_admin** (api-key + token pagination; page-2 carries the returned token VALUE), **openai_admin** (Bearer + `has_more`/`last_id`→`after` cursor, token-driven advance proven), **copilot** (Bearer + GH version header; top-level array → per-day emissions), **devin** (Bearer; operator-templated `org_id` `base_url`; pagination deferred), **granola** (Bearer; `since` watermark operator-side), **cursor** (Basic key-as-username; **POST** date-range body; PII-free), **servicenow** (Basic user+password; **offset** pagination advances `sysparm_offset` by `limit`, stops on a short page). Each carries **UNVERIFIED wire assumptions** (envelope key / cursor / body shape) recorded in its `auth.md` as the gate before live-network wiring — verify-before-cite; the wrong-envelope-key→0-emissions blast radius is regression-locked. FX-SEC-001 remains the un-bypassable emission backstop. **All 7 buildable poll connectors covered**; **deferred** (don't fit the page-list shape): google_drive (`documents.get`+OAuth), mcp_registry (Candidate). Independent pre-impl audits (VETO→addressed ×3) + pre-seal devil's-advocate. **Drift-corrected 2026-06-08 (Fix Cycle 1, doc-verification campaign PR #71):** devin envelope `items` (was `sessions`) + `pull_requests[].pr_url` + cursor pagination; granola host `public-api.granola.ai` + `/notes?include=transcript` + `notes` envelope + joined `transcript[].text` + `attendees` + `created_at` + cursor pagination — both were severe drift that would have ingested zero/wrong data live. **Fix Cycle 2 (2026-06-08):** anthropic_admin nested `cache_creation.{ephemeral_1h,5m}` token sum (was flat key → undercount); copilot `page`/`per_page` pagination wired via new `PageNumberPager` (top-level-array, stop-on-short-page); continue_dev `eventName`+`modelName` (was `name`); cursor pagination deferred-with-reason (page/pageSize transport unverified) + `name` dropped from docs. **Fix Cycle 3 (2026-06-08):** mcp_registry **graduated Candidate→Beta** — re-verified OpenAPI contract (public no-auth `GET /v0/servers`, `servers`/`element.server` envelope, `cursor`→`metadata.nextCursor`); new `NoAuth` + nested-path/optional-has_more `PageToken` (real consumers). **All 7 code-drift connectors fixed.** **Fix Cycle 4 (2026-06-08, doc-only):** corrected the verified doc-accuracy items — confluence deferral-reason (Connect-app JWT, not "no scheme"), google_drive scope (`drive.readonly`, not `drive.metadata.readonly`), fathom Svix-attribution-inferred note, sarif `level`, notion auth.md de-staled (Candidate→Beta + verified sig) — and recorded the pre-Live notes (sentry raw-body test, pagerduty browser sig-check, claude_code observed-schema). **Verification campaign COMPLETE: all 26 connectors doc-verified-and-correct.** L2 |
| FX-RUNTIME-004 | Operator-local config + headless runner (use connectors + mods WITHOUT the mcp UI) | docs/adr/0016-operator-local-config-and-runner.md, docs/plan-operator-local-runner-2026-06-08.md | runtime/local_config.py, runtime/runner_registry.py, runtime/cli.py, config/bicameral.example.json | runtime/tests/test_local_config.py, runtime/tests/test_cli.py | Verified | `python -m runtime.cli list\|run\|run-mods` drives connectors + mods from a gitignored `config/bicameral.local.json` (or env) — no UI blocker. `FileSecretResolver` (env `BICAMERAL_<KEY>` wins when set+non-empty, else flat file map; **never echoes a secret**); `load_config` fail-closed (non-dict/unknown-key/**duplicate-credential-key** rejected; `_`-prefixed comment keys allowed); `assert_runnable` HARD-fails token-free (KEY-NAME-only) on an unknown/missing-required credential for the target (required checked via the resolver so env-only passes). `runner_registry.RUNNERS` dispatches `linear`(GraphQL)/`google_drive`(documents.get)/7 REST poll connectors (absorbs `poll`'s connector-first-arg asymmetry); `load_mod` pins the manifest path. CLI default sink = local `CollectingSink` → prints **screened** emissions (source/title/excerpt — never a secret); `--sink gateway` = real POST (**default-gated**); `--limit` caps printed; `run-mods` pipes emissions through `run_mod` (dependency_risk). **Security:** secrets gitignored (**glob block** + `!example` negation) + env; a test proves the glob (3 variants) + a **secret-shape scan over tracked `config/*.json`** (independent of TruffleHog `--only-verified`); `main()`'s except handler prints `str(exc)` only (never `repr(config)`/token); `test_no_secret_in_stdout` captures stdout and asserts the value never leaks. Testable core takes injected transport/sink (a mock does NOT promote to Live — ADR-0012); only `main()` uses `UrllibTransport`. Independent audit (VETO→addressed: gitignore-glob, `--only-verified` backstop, fail-open run-validation) + pre-seal devil's-advocate (exhaustive secret-leak trace → PASS). Residual: linear over-requires the unused webhook secret for active-only runs (**resolved by FX-RUNTIME-005**); prefix-less secrets rely on the glob. L2 |
| FX-RUNTIME-005 | Multi-secret namespacing + mode-scoped required credentials | docs/plan-multi-secret-mode-scoped-credentials-2026-06-08.md, docs/adr/0016-operator-local-config-and-runner.md | runtime/local_config.py, connectors/_schema/connector-config.schema.json | runtime/tests/test_local_config.py, runtime/tests/test_cli.py | Verified | Closes the surfaced two-secret wire_gate. **Namespacing formalized** (`SecretResolver` docstring): resolve by credential **key** — single-secret = `source_id`, multi-credential = namespaced `<connector>[_<purpose>]` (`linear` + `linear_webhook`), globally unique by the `load_config` dup-key guard, env `BICAMERAL_<KEY>`. **Mode-scoped gate:** new optional `credentials[].modes` in the schema (enum webhook/active/passive); `assert_runnable(config, id, *, mode="active")` requires only credentials serving the run's mode — **absent OR empty `modes` = all-mode** (`mode in (c.get("modes") or [mode])`, closing the `modes:[]` silent-never-required hole) so an active `runtime.cli run linear` requires only `linear` (not the webhook secret, `modes:["webhook"]`); the unknown-key rejection stays **mode-independent** (typo'd key fails on any mode) and a genuinely-missing active credential still fail-closes at `_require_secret`. linear/google_drive descriptors gain per-credential modes; the validator adds a **`credentials[].modes ⊆ connector modes`** drift-guard (a credential serving an undeclared mode = dead = latent under-require). index.json + SETUP.md regenerated (SETUP.md shows "Serves run mode(s)"). The gate is **relaxed strictly** (fewer required); backward-compatible (descriptors without `modes` gate on all modes — the 24-connector fan-out). Residual: webhook-*receive* secret injection into `deliver_webhook` stays operator-runtime. Independent audit (VETO→addressed: `modes:[]` under-require, absent-modes backward-compat proof) + pre-seal devil's-advocate (gate-relaxation soundness → PASS). L2 |

---

## Section: Mods (EM-safe advisory post-processors)

| entry_id | feature | source_doc | source_file | test_path | status | notes |
|---|---|---|---|---|---|---|
| FX-MOD-001 | Mod execution contract — manifest-enforced, EM-safe runner for advisory mods | docs/adr/0013-mod-execution-contract.md, docs/adr/0002-em-safe-mod-manifest.md, docs/adr/0007-em-safe-mod-boundary.md, docs/plan-mod-execution-contract-2026-06-08.md | mods/contract.py, mods/_manifest.py | mods/tests/test_contract.py | Verified | `Mod` protocol (`id`/`version`/`outputs`/`evaluate`) + `ModEmission` (frozen; `__post_init__` binds `output_type`↔artifact — only `routing_hint`→`RoutingHint`, the other 5 kinds→`AdvisoryResult` by `kind`) + `run_mod`. EM-safe boundary: write-canonical/approve/resolve/block **non-representable** (return-only API), mutate-evidence impossible (frozen `AdapterEmission`/`SourceEvidence`); runtime checks — outputs allowlist, `id`/`version`/`outputs` mirror `manifest.yaml`, **no opaque numeric score** (dimensional `ConfidenceSurface` only), and **FX-SEC-001 `detect_sensitive` screen over every wire-bound mod-output field** (a mod that finds a secret can't surface it). `_EM_SAFE_FORBIDDEN` = full ADR-0007 7-action set; all 13 manifests normalized to declare it. Stdlib YAML-subset loader (`_manifest.py`; no PyYAML; CRLF/BOM-tolerant, str-version, fail-closed on nesting/tabs/dup-keys/unknown-keys/empty-forbidden). v1 manifest narrows ADR-0002/0007 (source-types/confidence-dims/audit-preservation deferred). Independent pre-impl audit (VETO→addressed) + pre-seal devil's-advocate. No mod logic yet (dependency_risk = next cycle). **Input boundary (ADR-0014, 2026-06-08):** `run_mod` now calls `validate_emissions(emissions)` BEFORE `evaluate` — a defensive input re-screen (mirrors `GatewaySink.emit`) so a hand-built emission carrying a secret in `metadata` (now preserved through `normalize`) can't reach a mod raw; the screen fires before `evaluate` runs (regression-locked). L1 |
| FX-MOD-002 | dependency_risk — the reference advisory mod (dependency vulnerabilities + manifest-change signals) | docs/plan-mod-dependency-risk-2026-06-08.md, docs/adr/0013-mod-execution-contract.md, docs/adr/0007-em-safe-mod-boundary.md | mods/dependency_risk/connector.py | mods/dependency_risk/tests/test_dependency_risk.py | Verified | `DependencyRiskMod` (`Mod` protocol; `outputs` exactly mirrors `manifest.yaml` = `dependency_signal`/`routing_hint`/`source_evidence_annotation`). First mod logic on the ADR-0013 contract; proves the ADR-0014 metadata unblock end-to-end (OSV fixture → `parse_vuln` → `normalize` → `run_mod` → `metadata["packages"]=="example-pkg"`). Two deterministic, stdlib-only, read-only paths: **vulnerability** (per `ev.source_ref.kind=="vulnerability"` — iterates ALL evidence — reads `emission.metadata` packages/severity/aliases → `dependency_signal` + security `routing_hint` (`priority="high"` iff a `CVE-`/`GHSA-` alias is present, per-token prefix split on `,`; **no CVSS-band fabrication**) + annotation) and **manifest-mention** (no-vuln emissions whose lowercased text contains a dependency-manifest filename as a contiguous substring → low-confidence `dependency_signal` + annotation, **no routing**, `evidence_ids=()`). Pure function; every output passes `run_mod` (outputs-allowlist + opaque-score + FX-SEC-001). 5 tests: OSV end-to-end, manifest-acceptance (set-equality), manifest-mention path, **no-false-positive** (`evaluate→[]`), secret-in-input-ref rejected (input screen fires before `evaluate`). Independent audit (VETO×1→addressed: multi-evidence iteration, evidence_ids framing, test adequacy) + pre-seal devil's-advocate PASS. README Outputs drift corrected (was `advisory_governance_result`). L1 |

---

## Section: CI & Governance Gates

| ID | Feature | Doc | Code | Test | Status | Notes |
|---|---|---|---|---|---|---|
| FX-CI-GOV-001 | Governance-integrity gate (ledger hash-chain + FEATURE_INDEX) | docs/plan-ci-governance-gates-2026-06-04.md | scripts/governance_gate.py | scripts/tests/test_governance_gate.py | Verified | stdlib; genesis-anchor rule (SG-2026-06-04-D); blocking via governance-gate.yml; verifies committed ledger + test paths |
| FX-CI-SEC-001 | Security + supply-chain gates | docs/plan-ci-governance-gates-2026-06-04.md | .github/workflows/codeql.yml | (D4.d waiver) | Verified | CodeQL/Bandit/dependency-review/Scorecard/SBOM/pip-audit/Dependabot, SHA-pinned; config validated by workflow-lint + tools executing |
| FX-CI-QUAL-001 | Quality/consistency gates | docs/plan-ci-governance-gates-2026-06-04.md | scripts/check_license_headers.py | scripts/tests/test_check_license_headers.py | Verified | workflow-YAML lint + codespell + SPDX-header scan (advisory) + conventional PR title |
| FX-CI-DOC-001 | Compliance framework mappings | docs/plan-ci-governance-gates-2026-06-04.md | docs/compliance/README.md | (D4.d waiver) | Verified | OWASP/NIST/EU-AI-Act/SOC2/GDPR+HIPAA control mappings; backbone is FX-CI-GOV-001; "alignment, not certification" |
| FX-CI-GOV-002 | Governance gate `--repo-root` (cross-repo verify) | docs/plan-reusable-gates-2026-06-04.md | scripts/governance_gate.py | scripts/tests/test_governance_gate.py | Verified | `main(--repo-root/--ledger/--feature-index)`; default unchanged; enables reusable cross-repo verification (SG-2026-06-04-E) |
| FX-CI-REUSE-001 | Reusable-workflow gate template | docs/plan-reusable-gates-2026-06-04.md | .github/workflows/_reusable-governance-gate.yml | (D4.d waiver) | Verified | portable gates as `workflow_call`; this repo dogfoods via thin callers; validated by workflow-lint + governance gate exit 0 |
| FX-CI-DOC-002 | Ecosystem gate-adoption guide | docs/plan-reusable-gates-2026-06-04.md | docs/ecosystem/consuming-gates.md | (D4.d waiver) | Verified | how bot/mcp/cloud consume the reusables + SHA-pin + --repo-root contract; backed by FX-CI-REUSE-001 |
| FX-CFG-001 | Connector config descriptor contract (the mcp-UI data contract) | docs/adr/0015-connector-config-descriptor-contract.md, docs/plan-connector-config-descriptors-2026-06-08.md, docs/UI_RENDERING_SPEC.md | connectors/_schema/connector-config.schema.json, scripts/validate_connector_config.py, scripts/build_connector_index.py, connectors/index.json | scripts/tests/test_connector_config.py | Verified | Per-connector `connectors/<id>/config.json` (machine-readable: identity/credentials/scopes/runtime_config/webhook+`receiver`/data/instructions/references/wire_gates) consumed by the mcp UI; this repo ships the **contract**, mcp **renders** it (`docs/UI_RENDERING_SPEC.md`); holds NO secret values. **Validator** (`validate_connector_config.py`, standalone `ci.yml` step — NOT in the cross-repo-portable `governance_gate.py`): fail-closed stdlib JSON-Schema-lite checker (rejects unknown keys, `mods/_manifest.py` discipline; stated subset, no `$ref`/`if`/`pattern`) + **code drift-guard** (`id`==folder==`source_id`; declared `modes` ⊆ connector `capabilities.modes` via fail-closed `importlib`; webhook block iff webhook mode; `instructions[].ref` REQUIRED for `open_url`/`register_webhook`/`configure` — anti-fabrication) + **index freshness** (committed `index.json` == fresh aggregation). **Reference-first exemplars:** Linear (api_key + webhook_secret — the two-secret resolver gap surfaced as a `wire_gate`, NOT hidden) + Google Drive (oauth2; `refresh_owner:operator`/`wiring_oversight:true` — UI owns consent, operator runtime owns refresh). 9 tests incl. the synthetic `modes⊄capabilities` reject against the real `LinearConnector`. Remaining 24 connectors fan out (batched, each validated on commit). Independent audit (VETO→addressed: governance-gate portability, missing webhook `receiver`, no-op drift-guard) + pre-seal devil's-advocate. **Backend how-to docs (2026-06-08):** `scripts/build_connector_setup.py` generates per-connector `connectors/<id>/SETUP.md` (operator runbook — prerequisites, where-to-get each credential, the config key + **per-credential `BICAMERAL_<KEY>` env var**, the local-config stanza with placeholders, webhook setup, the exact `runtime.cli` commands, go-live) **from `config.json`** (deterministic explicit-field access, `write_bytes` LF, `.gitattributes`-pinned); the validator byte-fresh-checks each. `docs/CONNECTOR_BACKEND_SETUP.md` = the hand-authored framework (config model, secrets, runner, webhooks, OAuth, go-live, troubleshooting with the real error strings). A `webhook_secret` credential is annotated **receive-path-only (NOT consumed by `runtime.cli run`)**. No secret values rendered (placeholders + a secret-shape backstop over the generated docs). Exemplars: linear + google_drive `SETUP.md`. Independent audit (VETO→addressed: per-credential env vars, webhook-secret trap, determinism, secret-shape, glob-scope) + pre-seal devil's-advocate. L2 |

---

## Gaps Surfaced

<!-- Reality without Promise / Promise without Reality entries land here. -->
