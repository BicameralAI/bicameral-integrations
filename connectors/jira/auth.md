# Jira Auth

Recommended v0: Jira Cloud API token plus account email using HTTP Basic auth.

Expected secret keys:

- `api_email`
- `api_token`
- `webhook_secret`

Dynamic webhook registration requires OAuth or Connect auth and is deferred.

