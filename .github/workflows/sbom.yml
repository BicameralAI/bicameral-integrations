name: SBOM

on:
  release:
    types: [published]
  workflow_dispatch: {}

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  sbom:
    uses: ./.github/workflows/_reusable-sbom.yml
    with:
      attest: ${{ github.event_name == 'release' }}
